import { Pane } from "./Pane.js";

export class HomePane extends Pane {
    constructor(paneId : string, paneButton : string) {
        super(paneId, paneButton);
    }

    updatePane() {
        
    }

}